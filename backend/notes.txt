async
callback function
event emitter
event handling


// globally nodemon :"npm i -g nodemon" - whenever we find the error "nodemon is not recognized" we have to install nodemon globally.

// to run:- npm nodemon server.js / npx nodemon server.js - (npx is used for running the package)

models in mongoose: to perform CRUD operations we need models over schema.

we can't make changes in hashing we can't 

# payload: 

9th july 2026
client                                     server 
-> generate a req                            -> give res on basis or req

# use case of middleware: to validate the request or to check if the token is valid or not 